name: Apply platform database patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-platform-box.yml'

jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply listener fix
        run: python .github/scripts/apply_platform.py
      - name: Verify and commit
        shell: bash
        run: |
          grep -q "electricityProvider','platform','pay'" index.html
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Fix platform fee live recalculation'
          git push
