name: Apply platform database patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-platform-box.yml'

jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Test runner
        run: echo "Platform patch runner is working"
